{
  "name": "global-payment-frontend",
  "version": "1.0.0",
  "description": "Global Payment App Frontend",
  "main": "index.html",
  "scripts": {
    "start": "npx live-server --port=3000 --no-browser",
    "dev": "npx live-server --port=3000"
  },
  "dependencies": {
    "axios": "^1.6.2",
    "qrcode": "^1.5.3",
    "socket.io-client": "^4.5.4"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
